# реф react?        
    https://github.com/MeetDOD/EduLearning/tree/main

Запуск через cmd:
    # активируйте nodevars.bat через терминал в папке проекта в моем случае команда активации будет такая:
    c:\My_Work_Space\Prog_tinkering\tools\node-v24.16.0-win-x64\nodevars.bat
    # проверьте версии npm и node
    npm -v
    node -v
    # Создай и запусти React-проект
    # Теперь разворачиваем сам React через современный сборщик Vite:
    # Создай шаблон проекта:
    npm create vite@latest my-react-app -- --template react

# установка npm
    cd my-react-app
    npm install

# запуск frontend
    # гайд говорит сделать так
    npm run dev -- --open --port 3000
    # я сделал так
    npm run dev


    